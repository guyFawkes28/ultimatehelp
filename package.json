{
  "name": "resort",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server --watch db.json --port 8080\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^0.17.4",
    "vite": "^7.1.7"
  }
}
